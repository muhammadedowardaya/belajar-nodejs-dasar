import process from "process";

const buffer = Buffer.from("Muhammad Edo Wardaya");

console.info(buffer);
console.info(buffer.toString());

console.info(buffer.reverse());
console.info(buffer.toString());
